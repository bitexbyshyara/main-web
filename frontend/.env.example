# Backend API base URL (shared with POS)
VITE_API_BASE_URL=http://localhost:8080

# POS website URL (for "Open POS Dashboard" cross-site links)
VITE_POS_URL=https://bitex-pos.shyara.co.in

# Razorpay Key ID (public key, safe to expose in frontend)
# The checkout hook gets this from the backend, but it's here for reference
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
