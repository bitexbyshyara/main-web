spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://dpg-d6dufvnfte5s73dliod0-a.oregon-postgres.render.com:5432/main_web_db_9nj2}
    username: ${DATABASE_USERNAME:main_web_db_9nj2_user}
    password: ${DATABASE_PASSWORD:T9aLK0TCIhyoBpijJRkH28HmWItnnAiE}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: ${PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:bitex-dev-secret-key-change-in-production-must-be-256-bits-long!!}
    expiration-ms: 86400000
  cors:
    origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:5174}
  razorpay:
    key-id: ${RAZORPAY_KEY_ID:rzp_test_placeholder}
    key-secret: ${RAZORPAY_KEY_SECRET:placeholder_secret}
    webhook-secret: ${RAZORPAY_WEBHOOK_SECRET:placeholder_webhook_secret}
  upload:
    dir: ${UPLOAD_DIR:./uploads}
